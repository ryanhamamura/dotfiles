PR checks pass. Let's squash and merge the PR with a clean, semantic commit message that includes the PR number. No Claude lines. 

Once you've switched back to main and pulled the latest changes, clean up the feature branches both locally and remotely and prune the remote. 
